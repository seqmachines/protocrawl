services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: protoclaw
      POSTGRES_PASSWORD: protoclaw
      POSTGRES_DB: protoclaw
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
